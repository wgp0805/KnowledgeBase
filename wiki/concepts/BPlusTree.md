---
title: "BPlusTree"
type: concept
tags: [数据结构, 索引, 关系数据库, MySQL]
sources: [raw/01-articles/拼多多二面：为什么要使用 ElasticSearch？和传统关系数据库 MySQL 有什么不同？.md]
last_updated: 2026-08-26
---

## 定义

B+Tree 是 MySQL（InnoDB 引擎）索引的底层数据结构。它将索引列的值排好序，查询时从根节点往下走，时间复杂度稳定在 O(log n)，等值查询和范围查询都非常快。其前提是 **能走上索引**。

## 关键信息

### 优势
- 等值查询、范围查询性能稳定（O(log n)）
- 有序性支持前缀匹配
- 适合事务性工作负载和 ACID 保证

### 致命短板：模糊搜索失效
```sql
-- 前置 % 导致索引失效，全表扫描
SELECT * FROM product WHERE title LIKE '%手机%';

-- 前缀固定可走索引
SELECT * FROM product WHERE title LIKE '手机%';
```
`%` 放在前面意味着开头可以是任意字符，B+Tree 的有序性完全用不上，优化器只能放弃索引走全表扫描。真实业务中用户搜索词位置随机，后置匹配根本不解决问题。

### 覆盖索引细节
若查询列都在某个二级索引里（覆盖索引），MySQL 有时会全扫描更小的二级索引树而非聚簇索引，代价小一些。但本质仍是"整棵树挨个摸一遍"，数据量大照样慢，没有质的区别。

### 与倒排索引对比
| 维度 | B+Tree | 倒排索引 |
|------|--------|---------|
| 擅长 | 等值/范围查询、事务 | 全文检索、模糊搜索 |
| 弱点 | 模糊搜索、分词 | 强事务、频繁更新 |
| 适用 | 关系数据库（MySQL） | 搜索引擎（ES） |

## 关联连接
- [[MySQL]] — B+Tree 的典型应用载体
- [[InvertedIndex]] — 对比数据结构（ES 索引底层）
- [[Elasticsearch]] — 搜索场景的替代方案
- [[摘要-拼多多二面-es-vs-mysql]] — 来源
