---
title: "Distro协议"
type: concept
tags: [一致性协议, 微服务, 服务发现, Nacos]
sources: [raw/01-articles/为什么越来越多人用Nacos？.md]
last_updated: 2026-08-05
---

## 定义
Distro 是 Nacos 自研的**最终一致性协议**，专门为服务发现场景设计，是 Nacos AP 模式（服务发现）的底层实现。

## 关键信息
### 责任分片
每个 Nacos 节点只负责一部分服务实例的"权威数据"，实现无主分布式的数据分流。

### 读写分离机制
- **写操作**：客户端注册到任意节点，节点先本地写，再**异步广播**给其他节点
- **读操作**：客户端从任意节点读取，读到的是该节点当前的本地数据（本地读，读很快）
- **最终一致**：一段时间内集群内同一服务实例视图会收敛
- **节点故障可恢复**：某节点挂了，它负责的实例数据由其他节点接管，重启后可重新拉取

### 设计哲学
Distro 承认"服务发现是 AP 场景"，没有为强一致硬上 Raft，牺牲短暂不一致换取高可用与高吞吐。

### 健康检查机制
| 健康检查类型 | 适用场景 | 工作机制 |
| --- | --- | --- |
| 客户端主动心跳 | 大多数场景 | 服务实例定期发送心跳，超时标记不健康 |
| 服务端主动探测 | 特殊场景 | 服务端主动探测实例端口是否可达 |
| TCP/HTTP 检测 | 定制化需求 | 通过 TCP 连接或 HTTP 请求检测可用性 |
| 自定义检测 | 特殊业务场景 | 支持用户自定义健康检查逻辑 |

## 关联连接
- [[Nacos]] — 所属系统
- [[摘要-为什么越来越多人用Nacos]] — 来源
- [[microservices]] — 微服务架构上下文
