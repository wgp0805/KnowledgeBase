---
title: "模型指纹"
type: concept
tags: [大模型, 模型识别, Tokenizer, 面试题]
sources: [raw/01-articles/“牛来”员工：你们可以在 OpenCode 爽用 Ox Alpha 模型了，1M上下文并支持视频输入（附Agent面试题）.md]
last_updated: 2026-08-24
---

## 定义
模型指纹指大模型在推理过程中不可避免暴露出的技术特征。这些特征由训练过程决定，不受 system prompt 和 API 影响，可用于识别匿名模型的真实技术来源——因为直接问"你是什么模型"不可靠（自我认知完全由训练数据和系统提示词决定，模型没有内省能力）。

## 五个识别维度
1. **Tokenizer 词表**：BPE 分词器词表由训练语料统计生成，同一家族模型（如 Qwen 系列）共享几乎相同的词表，相当于模型的"DNA"；重新训练 Tokenizer 意味着所有预训练权重作废，成本极高，因此同公司系列模型通常复用词表
2. **logprob 分布**：相同 prompt 下不同模型的 token 概率分布差异明显，采集数百条 prompt 的 top-5 logprobs 做统计比对可缩小候选范围
3. **特殊 token 格式**：不同模型的 `<|im_start|>`、`<|endoftext|>`、`<think>` 等控制 token 不一样
4. **知识截止日期**：询问有明确时间节点的事件，推断训练数据截止时间，结合厂商公开训练节点排除候选
5. **推理风格和格式偏好**：默认输出 Markdown 表格还是编号列表、是否先"让我想想"等行为特征

## 设计区分度测试的方法
- 构造探针文本集：多语言混合、生僻 Unicode（数学花体字母）、代码片段、长数字串、特定领域中文组合
- 采集两个确定值：token 数量、切分边界位置（Tokenizer 是确定性的，不受温度参数影响）
- 预先对已知模型建指纹库比对；token 数完全一致为强匹配，logprob 余弦相似度 >0.95 为中等匹配，知识截止+行为特征为弱匹配辅助排除

## 自动化应用："匿名模型验明正身 Agent"
三阶段设计：①并行采集四组探针证据（Tokenizer 探针取 usage 字段 / 100 条标准化 prompt 采 top-5 logprob 统计熵与集中度 / 20 个事实性问题测知识断点 / 格式行为测试）；②查指纹库交叉比对；③输出含置信度、分级证据链与备选假设的结构化报告。

## 关联连接
- [[摘要-ox-alpha模型与agent面试题]] — 来源
- [[OxAlpha]] — 文章以该匿名模型为引子
- [[RLHF]] — 自报身份可在对齐阶段被专门训练，故不可作凭证
- [[幻觉]] — 同属"模型输出不可作为事实凭证"的认知边界
