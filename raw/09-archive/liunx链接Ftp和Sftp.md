---
title: liunx链接Ftp
tags:
  - liunx
categories:
  - Liunx
date: 2022-06-20 20:14:29
---

# liunx链接Sftp

### linux sftp远程连接命令

　　`sftp -P 60001 root@192.168.0.254`

​		或者

​		`sftp  root@192.168.0.1` 也是可以的

　　使用`-P`选项来指定端口号。

　　sftp> get /var/www/fuyatao/index.php /home/fuyatao/

　　这条语句将从远程主机的 /var/www/fuyatao/目录下将 index.php

　　下载到本地 /home/fuyatao/目录下。

> ### 查看当前路径目录

```
pwd
```

>### 查看目录下所有文件

```
ls
//更改和/或打印本地工作目录
lcd 
```

> ### 列出本地目录内容

```
lls
```

> ### 打印远程工作目录

```
pwd
```

> ### 打印本地本地工作目录

```
lpwd
```

> ### 浏览您的本地目录，即打开本地目录

```bash
explore
```

> ### 将文件从本地计算机上载到服务器

```bash
#把本地文件test.txt上传到ftp服务器/tmp目录下
put ~/test.txt /tmp/
#把本地文件test上传到sftp服务器/tmp目录下
put -r ~/test /tmp/
```

> ### 在远程服务器上创建一个目录abc

``` java
mkdir abc
```

> ### 移动或重命名远程服务器上的文件

``` java
mv /test.txt /abc.txt
```

> ### 移动或重命名远程服务器上的文件

``` java
rename /test.txt /abc.txt
```

> ### 删除远程服务器上的文件abc.txt

```
rm abc.txt
```

> ### 删除远程服务器上的目录abc 

```
rmdir abc
```

> ### 批量下载

​		如果下载的文件夹是以日期命名的，这时想要下载一个月的，那么可以使用以下命令

```
// -r 就是下载文件夹下的所有文件及文件夹
//*这里是匹配*前边想同的文件夹进行下载

get -r 202203*
```

​		sftp> put /home/fuyatao/downloads/Linuxgl.pdf /var/www/fuyatao/

　　这条语句将把本地 /home/fuyatao/downloads/目录下的 linuxgl.pdf文件上传至远程主机/var/www/fuyatao/ 目录下

> ### 清楚屏幕

```
clear
```

# liunx链接Ftp

以前操作远程ftp上的文件和文件夹都是通过ftp工具去实现的，但是一直没尝试过再liunx上实现过操作ftp，直到今天接到了处理ftp上一些文件的任务才发现这东西操作起来还和其他系统上有点不一样，接下来见到记录下ftp相关的命令操作，

### 链接Ftp服务器

再网上看到了好多都说链接ftp的格式如下

```
ftp [主机名称| IP -地址]
```

但是这个格式实际上用的比较多的就是ip，使用地市名称感觉还是用的比较少

`ftp 192.168.0.1 ` 然后回车就会让你输入用户名和密码，然后直接输入对应的用户名密码就可以进入对应的ftp页面,验证通过后就会出现如下的操作行

```
ftp>  .........
```

### 查看文件列表和切换目录

`cd` 切换目录命令 

`ls` 展示目录上的所有内容

我在我的ftp上操作时发现每次操作下边都会显示一个状态码和一些描述，不知道是不是我的ftp就这样

### 下载文件

要下载文件，通常使用两个命令： get和mget。

> get

格式： `get [远程- file ] [ local-file ]`

将文件从远程主机传输到本地主机。

要获取远程服务器上的/usr/your/test.txt，

操作如下 

```
ftp> get /usr/your/test.txt test.txt(回车)
```

> mget

格式： `mget [远程- files ]`

从远程主机向本地主机接收文件批处理。

要获取服务器上/usr/your/下的所有文件，请

```
ftp> cd /usr/your/
ftp> mget *.* (回车)
```

此时每次下载文件都会有提示。 若要移除提示，请在mget *.*命令之前运行：prompt off

注：所有文件都已下载到linux主机的当前目录中。 例如，对于在/usr/my下运行的ftp命令，所有文件都将下载到/usr/my下。

### 显示下载进度

默认的情况是什么都看不见的，在这瞎等也属实是啥都不敢动

> hash

```
ftp> hash
```

这样就可以显示下载的进度了

### 上传文件

> put

格式：

`put local-file [remote-file]`
将本地一个文件传送至远端主机中。
如要把本地的test.txt传送到远端主机/usr/your,并改名为test2.txt，则：

```
ftp> put 1.htm /usr/your/test2.txt(回车)
```

> mput

格式：`mput local-files`
将本地主机中一批文件传送至远端主机。
如要把本地当前目录下所有html文件上传到服务器/usr/your/ 下，则：

```
ftp> cd /usr/your （回车）
ftp> mput *.htm　（回车）
```

注意：上传文件都来自于主机的当前目录下。比如，在　/usr/my下运行的ftp命令，则只有在/usr/my下的文件linux才会上传到服务器/usr/your 下。

### 断开链接

> bye   exit   quit

中断与服务器的连接。这三哥命令都可以执行退出

```
ftp> bye：中断与服务器的连接。
```

### 常见的状态指令

这里只列举部分常见的状态执行含义，如果遇到其他的请自行百度

> 1xx - 肯定的初步答复这些状态代码指示一项操作已经成功开始，但客户端希望在继续操作新命令前得到另一个答复。

- 110 重新启动标记答复
- 120 服务已就绪，在 nnn 分钟后开始

> 2xx - 肯定的完成答复 一项操作已经成功完成。客户端可以执行新命令。

- 200 命令确定
- 202 未执行命令，站点上的命令过多
- 212 目录状态
- 213 文件状态
- 230用户已登录，继续进行
- 250 请求的文件操作正确，已完成

> 3xx - 肯定的中间答复,该命令已成功，但服务器需要更多来自客户端的信息以完成对请求的处理

- 331 用户名正确，需要密码
- 332 需要登录帐户

> 4xx - 瞬态否定的完成答复,该命令不成功，但错误是暂时的。如果客户端重试命令，可能会执行成功

- 426 Connection closed; transfer aborted
- 451 请求的操作异常终止：正在处理本地错误

> 5xx - 永久性否定的完成答复,该命令不成功，错误是永久性的。如果客户端重试命令，将再次出现同样的错误

- 500 语法错误，命令无法识别。这可能包括诸如命令行太长之类的错误
- 501 在参数中有语法错误
- 502 未执行命令
- 503 错误的命令序列
- 504 未执行该参数的命令
- 530 - 该状态代码表示用户无法登录，因为用户名和密码组合无效。如果使用某个用户帐户登录，可能键入错误的用户名或密码，也可能选择只允许匿名访问。如果使用匿名帐户登录，IIS 的配置可能拒绝匿名访问
- 550 未执行请求的操作。文件不可用(例如，未找到文件，没有访问权限)

